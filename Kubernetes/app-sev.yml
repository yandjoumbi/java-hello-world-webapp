# apiVersion: v1
# kind: Service
# metadata:
#   name: app-svc
#   labels:
#     run: app
# spec: 
#   ports:
#   - port: 8080
#     protocol: TCP
#   selector: 
#    run: app
#   type: LoadBalancer


apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  ports:
  - port: 80
    targetPort: vproapp-port
    protocol: TCP
  selector:
    app: vproapp
  type: LoadBalancer